You are implementing Milestone M17 ("What your life sounds like" — making the on-device sound classifier's 93 labels visible, honest and useful, and stopping the app from destroying a recording mid-take) for the Soundpost iOS app at /Users/jason/Documents/Soundpost.

START BY READING docs/M17-DEVPLAN.md in full — it is the plan after a three-party review (Gemini 3.1 Pro + Gemini 3.7 Flash + an adjudicating Claude Opus 4.6 pass), whose §12 records what they found, where they disagreed, and what got overturned. Follow it exactly; if you deviate, say why. Then skim docs/PROJECT.md (the vision §1c and the honest-limits ethos §1e) and docs/M16-DEVPLAN.md (the milestone that just shipped — §13 is its build record, including where its own plan was wrong, and §11 is what M17 was originally going to be). Then read docs/M15-DEVPLAN.md §11P–§11R: it is the discipline this repo runs on and it names the two human steps still outstanding. Then read the code the plan cites: Models/Soundprint.swift, Models/SoundVocabulary.swift, Services/SoundprintRemediation.swift, Services/SoundprintBackfill.swift, Services/SoundprintEraser.swift, Services/GalleryBrowsing.swift, Services/CapsuleBulkExporter.swift, Services/NotificationCopy.swift, Audio/AudioRecorder.swift, Capture/CaptureViewModel.swift, Capture/CaptureView.swift, ContentView.swift, Views/CapsuleCard.swift, Views/CapsuleDetailView.swift, and Services/ListeningConsentStore.swift (its `set` doc comment is the evidence that decided §4B).

WHERE THINGS STAND: master @ `b9cacec`, MARKETING_VERSION 1.7.0 / build 16, working tree clean. 440 tests in 56 suites / 0 warnings / i18n EN·JA·ZH-Hans 100% across 322 strings / 93 sound labels / CloudKit seed coverage — all gates green on a clean local build. **CI has NOT run since `b025459`; master is 15 commits ahead of `origin/master`.** Two warnings once shipped into master through exactly that gap, so do not treat "gates green" as verified until it is pushed — that is Jason's step, not yours. 1.6.1 is live, 1.6.2 is in App Review, and **1.7.0 is code-complete but NOT uploadable**: it needs `CD_ListeningConsent` created in CloudKit Development (any iOS Simulator signed into iCloud will do; signing in needs Jason's password) and then a promotion. A second, independent promotion is authorised and outstanding: `CK_ALLOW_PARTIAL=yes CK_CONFIRM=yes scripts/cloudkit-schema.sh promote`, which fixes a live defect where cloud-backed far-future delivery has never worked in any shipped build. **Neither is M17's job and neither blocks M17.** Do not touch the 1.7.0 consent work, the in-review 1.6.2, or the M10 delivery backend.

THE DECISION THAT SHAPES EVERYTHING — read §4B before you write a line. The plan originally proposed letting people reject a wrong sound label by storing a tombstone inside the existing `soundprintRaw` string, so the milestone would need no CloudKit schema change. **The review overturned it.** One reviewer endorsed it on the premise that CloudKit merges field-by-field; that premise is false, and this repo already established it at cost: `ListeningConsentStore.swift:136` records that `NSPersistentCloudKitContainer` resolves conflicts with record-level last-writer-wins, which is why account-wide consent was rebuilt from one mutable row into immutable append-only rows. A rejection in `soundprintRaw` has the identical failure mode and a worse consequence — a stale **machine guess** silently overwriting an explicit **user rejection**, with no event the user can attribute. Three code paths were also found that destroy such a tombstone outright (a gate bump, a consent off/on cycle, and `isEmpty` lying). **So M17 ships the labels visible and attributed, with NO per-label correction, and rejection is M18's first item behind the promotion as immutable `SoundRejection` rows.** If you find yourself reaching for the tombstone anyway, re-read `ListeningConsentStore.swift:133–150` first.

HARD CONSTRAINTS (do not violate):
- **No new CloudKit field, no new entity, no schema promotion.** `soundprintRaw` already exists on `Capsule` and already syncs; that is exactly why M17 can run while 1.7.0 is blocked. If a step seems to need a field, STOP — and note that `cloudkit-schema.sh promote` cannot promote a field-only change at all: it prints "Nothing to do — Production already matches" and exits 0 at line 176, *before* the importer at line 186 (§4F).
- **A label is a guess and the UI must say so in its own shape** (§4A): attribution in the copy ("Soundpost heard"), never a bare noun where the mood sits; **on the card only when the capsule has no note** — the machine's guess fills a silence, it never sits above the user's own words (this is `NotificationCopy.Digest.lead`'s existing precedence, reused); never on a sealed-not-due capsule; never when listening consent is off; never on the share card, the video, or the note.
- **Delete no audio files.** The orphan leak is real and gets *counted*, not swept — see §4E for why the draft's sweep was the most dangerous line in the plan.
- Free stays free. No Pro lever — the paywall is a dead end until a human fixes App Store Connect (§8.2).
- Standing bars at EVERY commit: warning-free on Xcode 26, all tests green, i18n 100%, zero new dependencies, CI green. Each step compiles, passes, and is COMMITTED before the next. End each commit message with:
  Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>

SEQUENCE — S0 → S1 → S2 → S3 → S4 exactly as in §5:
- **S0 protect the recording.** Wire `AudioRecorder.cancel()` (it exists and has zero callers) into the discard path; `interactiveDismissDisabled` while a take is in flight; confirm before throwing one away; add a read-only orphan **count** at launch. Delete nothing.
- **S1 integrity of what Soundpost heard.** No UI. Consent-gate `CapsuleBulkExporter`; give `Soundprint` an explicit *showable-labels* notion and move every render site onto it; make `SoundprintRemediation` gate-aware — `legacyPrefix()` hard-codes `"1/version1|"`, so no gate-2 verdict can ever be reopened and a gate-3 bump would strand every gate-2 capsule silently.
- **S2 the label made visible, attributed.** Detail view first, then the card under §4A's rules.
- **S3 find others that sounded like this.** A `sounds` facet on `GalleryFilter.Criteria`; tapping a phrase in detail returns to the gallery with that facet active and a removable chip. **Do not enumerate a facet list** — the chip is the one the user tapped.
- **S4 the honesty sweep.** Notification authorization state; `seal(until: today)` storing a past instant; the cold-launch deep link; the mood-filter empty state that claims to be a search result; tappable "Coming up" **echo** cards (not seal cards — their capsule is hidden).

Drop order if tight: S4, then S3. **Never drop S0 before S2** — a milestone that invites more recording while a swipe still destroys a take is the wrong trade.

THE TRAPS THE REVIEW SURFACED (§4 / §7 — these will pass a green suite while being broken):
- **A sweep that deletes files no capsule points at deletes the user's live recording.** The `.m4a` exists from record *start*; the `Capsule` row only from *save* (`CaptureViewModel.swift:219`). A test that drops a stray file next to a saved capsule passes green and never simulates a take in flight. This is why S0 counts and does not delete.
- **`Soundprint.isEmpty` is `labels.isEmpty`** (`Soundprint.swift:144`) — it counts labels, not *showable* ones. A value holding only out-of-vocabulary or below-floor labels reports non-empty while every display path renders nothing, so `CaptureView.swift:217` already draws a "Sounds like" header over zero chips. After S2 that ghost would appear on every new surface.
- **`SoundprintRemediation.rejudge` filters survivors through `SoundVocabulary.isAllowed` and the current floor** (`:63`). That is correct and it is also what would have stripped any tombstone — the same mechanism that makes §4B unworkable. Fixing the gate-blindness without understanding this is how a "fix" quietly deletes data.
- **Deriving a facet list by walking the library inside `body`** runs 20×/second while a capsule plays (`AudioPlayer.swift:96`), on a path that already re-walks several times per pass. And M16 §7 forbids memoising on `sealSignature` — it omits content, so the list would freeze.
- **The cold-launch deep link never fires.** `pendingDeepLinkCapsuleID`'s only consumer is `.onChange` (`ContentView.swift:100`), which does not fire for a value already set at first body evaluation. Coordinator unit tests pass because they assert the id was set.
- **`interactiveDismissDisabled` cannot be unit-tested here.** There is no UI-test target. Verify it by hand in the simulator and say that is what you did.

THE DISCIPLINE THIS REPO RUNS ON — read docs/M15-DEVPLAN.md §11P before you write a gate or a test. The same structural bug was found FIVE times in one review: *a check that iterates an artefact cannot fail for what is missing from the artefact.* The localization gate walked the string catalog, so strings never added to it shipped untranslated. The CloudKit gate derived expected types from `Schema([...])`, so a hand-rolled record type sat missing from Production for months. `-only-testing:` on a suite that does not exist still prints `TEST SUCCEEDED` over `Executed 0 tests`. So: **every new test must be run against the pre-fix code and SEEN to fail before you trust it green** — for genuinely new code, break the implementation deliberately and confirm red, and record which mutation made which test fail. Always confirm the test COUNT, not the exit code. Local gates: `./scripts/check-localization.sh`, `./scripts/check-warnings.sh build/X.log` (needs a CLEAN build — it passes vacuously on an incremental one), `./scripts/check-sound-vocabulary.sh`, `./scripts/cloudkit-schema.sh check-seed`.

TEST-RUN GOTCHAS THAT HAVE EACH COST REAL TIME: run with `xcodebuild test -project Soundpost.xcodeproj -scheme Soundpost -destination 'platform=iOS Simulator,name=iPhone 17'`. Never pass `CODE_SIGNING_ALLOWED=NO` (the Sentry dylib goes unsigned and the host SIGTRAPs); never pass `-derivedDataPath` inside the repo. **Async tests must use `TestSupport.isolatedStore()`, never `freshStore()`** — the latter is a container-wide delete on a container every suite shares; M16 converted the eleven async callers that were still on it after adding one new suite turned `SealDeliveryTests` red in a way that read as a product bug. `VideoGateTests.playbackStaysFree` can fail LOCALLY ONLY (simulator audio) and is green on CI — confirm against an untouched baseline before chasing it.

HUMAN-GATED — STOP and flag Jason when you reach these: §4B's outcome if you think the review got it wrong (labels visible with no correction); anything that appears to need a CloudKit schema change; and the ASC privacy nutrition label check in §6, which is server-side state nothing in the repo can read (§11A#12 is open because that assertion was once made without checking). §8 lists the rest, including the one human hour in App Store Connect that would make the app sellable.

Do NOT: build any Pro feature (the paywall cannot load products — both IAPs are `MISSING_METADATA`); add a UI-test target (there is none — assert on the view-model/policy layer and do not claim UI coverage the project cannot run); add a widget, Control Center control or any app-extension target (needs an App Group and breaks the project's file-system-synchronized groups); ship a "capture today" notification (§0B — the decision is Jason's and it is not in scope either way); raise the deployment target above iOS 17; do any App Store Connect work or a release/submission; touch the 1.7.0 consent code or the in-review 1.6.2.

After each step, report what you did, the test result and count, which control mutation you used to see each new test fail, and the commit hash.
