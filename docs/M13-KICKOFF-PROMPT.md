You are implementing Milestone M13 ("The shareable waveform video" — the Pro video-export monetization lever) for the Soundpost iOS app at /Users/jason/Documents/Soundpost.

START BY READING docs/M13-DEVPLAN.md in full — it is the precise, Codex-and-Claude-review-hardened plan (its §12 records the review); follow it exactly, and if you ever deviate, say why. Then skim docs/PROJECT.md (the vision §1c + the honest-limits ethos §1e), docs/M11-DEVPLAN.md (the Pro/gating/share model this extends), and the code the plan cites: Views/ShareCardView.swift, Views/WaveformView.swift, Views/CapsuleDetailView.swift, Services/CapsuleExporter.swift, Services/CapsuleBulkExporter.swift (the M12 off-main/temp-file discipline to mirror), Models/ProGate.swift, Models/Capsule.swift, AudioSelfTest.swift (the headless self-test pattern for S0).

WHERE THINGS STAND: M12 SHIPPED to the codebase (200 tests / 0 warnings / i18n EN·JA·ZH-Hans 100% / zero new third-party deps beyond Sentry). CI is LIVE & GREEN (github.com/JasonYeYuhe/Soundpost, on macos-26 / Xcode 26.5) and gates every push on: the unit suite, warning-free project build, and 100% xcstrings. dSYM upload is LIVE (Sentry org jason-yeyuhe / project soundpost; SENTRY_AUTH_TOKEN in ~/.zshrc; build-upload-asc.sh auto-uploads). The live App Store version is 1.1.0; 1.3.0 (build 7) is in App Review; 1.4.0 (build 8) is uploaded/VALID. Both Pro products exist in ASC. Deployment target is iOS 17.0. Do NOT touch the M10 delivery backend, the M11 Pro gating, or the in-review 1.3.0.

THE DECISION THAT SHAPES EVERYTHING: M11 made a static card+audio share; M12 made the resurface moment land. M13 makes the payoff SHAREABLE — a short branded .mp4 whose waveform lights up in sync with the audio, ready for iMessage / a feed / (vertical) Stories/Reels. It is a Pro CREATION richness: additive, lapse-safe, NEVER a charge to receive. It is also the app's riskiest engineering (net-new AVFoundation encoding), so the plan DE-RISKS FIRST: an S0 render-proof spike must export a real, non-black, audio-synced .mp4 on the CI sim BEFORE any geometry/UI/tests are built on top.

HARD CONSTRAINTS (do not violate):
- Never charge to receive — video is Pro CREATION only; reveal/browse/notifications/export-your-data/playback stay free (add regression tests proving so). A sealed-not-due capsule structurally cannot be video-exported (no export affordance on the locked view).
- Calm, no dark patterns — a gentle left→right playback-position reveal; keep the "Made with Soundpost" mark; a paid "remove watermark" lever is RULED OUT.
- Privacy-first — no tracking (Sentry crash only); nothing new leaves the device except the user's explicit share. Keep two axes separate: Photos "Save Video" may need NSPhotoLibraryAddUsageDescription (a runtime permission — verify on a real iOS-17 device, add+translate EN·JA·ZH if required); the size preflight must be ARITHMETIC-ONLY (no free-disk-space query — that is a Required-Reason API, declare NSPrivacyAccessedAPICategoryDiskSpace only if ever added).
- Offline-first, no backend churn — 100% on-device; M10 backend + M11 gating untouched.
- No regression / standing bars: WARNING-FREE on Xcode 26 (this rules out the deprecated AVFoundation paths — see the traps), ALL tests green, i18n EN·JA·ZH-Hans 100% EVERY step (the localization gate reds CI on any untranslated key), ZERO new third-party deps (AVFoundation is first-party), CI green (macos-26), crashes symbolicated.
- Each step (S0→S4) compiles + passes tests + is COMMITTED before the next. End each commit message with:
  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>

WATCH THE REVIEW-SURFACED TRAPS (the P0/P1s in §4/§5/§7 — do not miss them):
- Render path (P0): use AVAssetWriter + AVAssetWriterInputPixelBufferAdaptor (+ AVAssetReader for the audio mux) — NOT AVVideoCompositionCoreAnimationTool. The burn-in path has no video track to composite over (audio-only composition → black/audio-only output), uses the wrong initializer, AND AVMutableVideoComposition / exportAsynchronously / .progress / cancelExport() are DEPRECATED on the iOS 26 SDK (deprecation warnings break the warning-free bar). AVAssetWriter is iOS-17-safe, warning-free, deterministic/testable, codec-controlled, memory-bounded.
- Sync (P0): key the composition duration, frame count, reveal, and tests to the LOADED audio-track duration (try await audioTrack.load(.timeRange).duration) — NEVER the stored Capsule.durationSeconds (they can differ by AAC priming/rounding). Name it "playback-position" sync, not amplitude sync.
- Warning-free (P0): in tests use await asset.load(.tracks)/.load(.duration) (the sync accessors are deprecated). No deprecated composition/export APIs anywhere.
- Test tiers (P1): CI is simulator-only and CoreAnimationTool-style renders can go black on the sim — but that's moot with AVAssetWriter; still, S0 must assert H.264 + a NON-BLACK first frame on the sim (structural test), and S2's pixel/reveal assertions (via AVAssetImageGenerator OVER THE EXPORTED .mp4, tolerances .zero, sample t=0 / midpoint / duration−frameDuration; non-black, stable card ROI, monotonically-increasing reveal coverage) plus a human-gated DEVICE smoke test. A purely structural S1 test can pass on a broken (black) export — so S0's non-black proof is mandatory.
- Orientation (P1): draw into an explicit top-left CGContext (no CALayer y-flip); render the video card via ShareCardView with a new showsWaveform:false + explicit lineLimit (else two waveforms + cropped long/localized notes). Verify card-upright + reveal-direction in the S2 device smoke test.
- Gate before the menu (P1): a free user's export tap goes STRAIGHT to the paywall (unchanged, no tease); only Pro users see the image/video menu. Fold eligibility into a testable action policy (content-visible + valid audio + Pro-at-start), captured once, never re-checked mid-render.
- Temp lifecycle from S1 (P1): a UNIQUE export dir per run (the current audioFileURL date-only filename collides); retain the .mp4 until UIActivityViewController.completionWithItemsHandler, then clean; clean on failure/cancel; scavenge stale dirs on launch.
- Preflight (P2): estimate size from a VIDEO-bitrate constant for the preset × duration (NOT CapsuleBulkExporter's 8 KB/s audio figure); arithmetic-only.

SEQUENCE: implement S0→S4 exactly as in §5. Committed core = S0–S4 (M13 is deliberately narrow — no splittable tail). Drop order if tight: downgrade S2's per-bar reveal to a single sweeping playhead line. NEVER drop: S0 (render proof), S1 (pipeline + temp lifecycle), S3 (the gate), S4's cancel/cleanup/error-surfacing. After each step, report what you did, the test result, and the commit hash. Keep CI green (push to master triggers it).

HUMAN-GATED — STOP and flag Jason when you reach these:
- Confirm the overridable product decisions (§8): vertical 1080×1920 v1 (recommended, matches the Stories/Reels ROI) vs square; keep AVAssetWriter primary (S0 may override with a documented reason); no paid brand-off; a duration cap for very long clips is decided AFTER S0/device render-time measurement.
- Photos "Save Video" permission: verify on a real iOS-17 device whether NSPhotoLibraryAddUsageDescription is required; if so, add INFOPLIST_KEY_NSPhotoLibraryAddUsageDescription + a translated InfoPlist.xcstrings entry (EN·JA·ZH) before S3 ships the share UI.
- The S2 on-device smoke test is human-gated (the sim can't fully judge visual sync/legibility): a real capsule exported + played back with synced, upright animation.
- The S0 render-path decision (AVAssetWriter vs the CoreAnimationTool synthetic-track alternative) is recorded in the plan with its exit criteria.

Do NOT: raise the deployment target above iOS 17 (the AVAssetWriter path needs no iOS-18 API); add the Pro micro-levers (custom mood color / custom echo window are M14 — see §11, the echo window is NOT lapse-safe like a rendered preference); do any App Store Connect product work or a release/submission (video is the existing Pro entitlement; the 1.4.0→next submission stays gated on 1.3.0 clearing review, separate from M13); revive the "remove watermark" lever (ruled out). The plan was hardened by a Codex pass + a 4-lens Claude audit; the Gemini CLI is tier-ineligible (see §12) — if you want a Gemini pass, Jason can paste docs/M13-DEVPLAN.md into Gemini web/Antigravity.
